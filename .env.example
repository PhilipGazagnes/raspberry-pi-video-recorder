# Environment Variables - COPY THIS TO .env AND FILL IN YOUR VALUES
# IMPORTANT: Never commit .env to version control!

# ============================================================================
# YouTube Upload Configuration
# ============================================================================

# OAuth Credentials (file-based authentication)
# Download client_secret.json from Google Cloud Console:
# 1. Go to: https://console.cloud.google.com/
# 2. Create a project (or select existing)
# 3. Enable YouTube Data API v3
# 4. Create OAuth 2.0 credentials (Desktop app type)
# 5. Download the client_secret.json file
# 6. Place it in the credentials/ directory

YOUTUBE_CLIENT_SECRET_PATH=credentials/client_secret.json

# Token file (generated automatically during initial setup)
# Run: python setup_youtube_auth.py
# This will create token.json with your authenticated session
YOUTUBE_TOKEN_PATH=credentials/token.json

# YouTube Playlist ID (optional)
# Find this in YouTube Studio > Content > Playlists
# Format: PLxxxxxxxxxxxxxxxxxxxx
# Leave empty to upload without adding to a playlist
YOUTUBE_PLAYLIST_ID=

# ============================================================================
# Upload Settings (optional - defaults in config/settings.py)
# ============================================================================
# Uncomment to override defaults:
# UPLOAD_CHUNK_SIZE=10485760  # 10 MB chunks
# UPLOAD_TIMEOUT=600          # 10 minutes timeout

# ============================================================================
# Setup Instructions
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Download client_secret.json from Google Cloud Console
# 3. Place it at credentials/client_secret.json
# 4. Run: python setup_youtube_auth.py (authenticate once)
# 5. Your .env is ready - token.json will be auto-generated
# 6. Start your application!

# Monitoring Settings (optional - defaults in settings.py)
HEARTBEAT_INTERVAL=1.0      # Heartbeat write frequency (seconds)
