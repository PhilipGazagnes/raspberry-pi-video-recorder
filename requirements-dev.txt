# Development dependencies for linting and formatting
ruff>=0.1.0
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0

# Optional: Additional development tools
pytest>=7.0.0      # For testing (if you add tests later)
